# Camera_App

A simple and user-friendly photo gallery app built using Flutter. It supports Google and email login, letting users capture, view, delete, and restore photos stored locally with SQLite. The clean, responsive UI includes a profile page, recently deleted section, and smooth navigation drawer.
